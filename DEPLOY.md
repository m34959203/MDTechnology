# 🚀 Деплой на Render.com

## Автоматический деплой через render.yaml

Проект уже настроен для автоматического деплоя на Render.com с помощью файла `render.yaml`.

### Шаги для деплоя:

1. **Перейдите на Render Dashboard**
   - Откройте https://dashboard.render.com/

2. **Подключите репозиторий**
   - Нажмите "New +" → "Blueprint"
   - Выберите "Connect a repository"
   - Авторизуйтесь через GitHub/GitLab
   - Выберите репозиторий `MDTechnology`

3. **Render автоматически найдет render.yaml**
   - Render обнаружит файл `render.yaml` в корне проекта
   - Нажмите "Apply" для создания сервиса

4. **Дождитесь завершения деплоя**
   - Render автоматически выполнит:
     - `npm install`
     - `npm run build`
     - `npm start`
   - Процесс займет 3-5 минут

5. **Получите URL вашего сайта**
   - После успешного деплоя вы получите URL вида:
   - `https://mdtechnology-ai.onrender.com`

---

## Ручной деплой (альтернатива)

Если вы хотите настроить деплой вручную:

1. **New Web Service**
   - На Render Dashboard нажмите "New +" → "Web Service"

2. **Настройки:**
   - **Name**: `mdtechnology-ai`
   - **Region**: Frankfurt (или ближайший)
   - **Branch**: `claude/mdtech-ai-website-01WZMSKdsAhYnP2tLPgpKh6c` (или main после мержа)
   - **Runtime**: Node
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Free

3. **Environment Variables** (опционально):
   ```
   NODE_VERSION=20.11.0
   NODE_ENV=production
   ```

4. **Deploy**
   - Нажмите "Create Web Service"

---

## После деплоя

### Проверьте сайт:
- Откройте ваш URL
- Проверьте все секции
- Убедитесь, что 3D компонент Spline загружается
- Проверьте ссылки на WhatsApp

### Настройте кастомный домен (опционально):
1. В настройках сервиса на Render
2. Перейдите в "Settings" → "Custom Domain"
3. Добавьте ваш домен (например, `mdtechnology.ai`)
4. Настройте DNS записи согласно инструкциям Render

### Автоматические обновления:
- Каждый push в ветку автоматически запускает новый деплой
- Вы можете настроить Auto-Deploy в настройках сервиса

---

## Полезные ссылки

- 📊 [Render Dashboard](https://dashboard.render.com/)
- 📖 [Render Docs - Next.js](https://render.com/docs/deploy-nextjs-app)
- 🔧 [Render Blueprint Spec](https://render.com/docs/blueprint-spec)

---

## Troubleshooting

### Проблема: Сборка не удалась
**Решение**: Проверьте логи сборки в Render Dashboard. Убедитесь, что Node.js версия 20.x.

### Проблема: 3D Spline не загружается
**Решение**: Убедитесь, что нет блокировки CORS. Spline работает через CDN.

### Проблема: Медленная загрузка
**Решение**: На бесплатном плане Render "засыпает" после 15 минут неактивности. Первая загрузка может занять 30-60 секунд.

---

## Upgrade to Paid Plan

Для production использования рекомендуется платный план:
- ✅ Нет "засыпания" сервиса
- ✅ Больше ресурсов (RAM, CPU)
- ✅ Автоматическое SSL
- ✅ CDN
- ✅ Приоритетная поддержка

**Стоимость**: от $7/месяц

---

🎉 **Готово! Ваш сайт MDTechnology.ai теперь в продакшене!**
